# PRD.md 自检报告（AI 自检，Day 4 · 第三版）

> 自查对象：`心屿/PRD.md` **v3.0**
> 核对标准：① Day 4 交付标准 ② 老板本轮三条指令（对齐 research.md / 单日可拆解 / 清空泛表述）③ 老板本轮四条收尾检查
> 结论：**3 个「必须修改」级问题（已当场修正），0 个遗留阻塞**；另记录 2 个我主动指出的文档自身隐患

---

## 一、老板本轮四条检查（逐条给证据，不给结论）

### 检查 1 · 随机挑一个功能，能回答「谁、在什么页面、做什么、看到什么」

**抽查对象：F2 情绪自评**（随机挑的，非挑选过的简单项）

| 问题 | 答案 |
|---|---|
| **谁** | 隐隐觉得自己状态不对、但不知道要不要找人聊的 20 岁上下大学生 |
| **在什么页面** | 首页功能卡 → `checkup.html`（作答与结果同页） |
| **做什么** | 回答 6~8 个「最近两周」状态的条目，一屏一题，可跳过任一题 |
| **看到什么** | 一段柔和建议（`normal` / `attention` / `crisis` 三种固定文案之一）+ 一个「看看能找谁」按钮；**页面上没有任何数字、没有任何分级词** |

✅ **通过**。且全部 8 个 P0 功能（F1~F9，F4 为组件）均采用同一四段式结构，位置：第 149 / 177 / 205 / 229 / 255 / 284 / 311 / 331 行。

### 检查 2 · PRD 有 MVP 和后续分界

| 分界 | 内容 | 位置 |
|---|---|---|
| **MVP** | F1~F9 共 9 个功能，**全部标 P0**（v3.0 取消了 P1/P2，因为「可降级」在 20 天排期里约等于事实不做） | 第 5 节 |
| **MVP 页面** | 11 个页面，全部 P0 | 第 6 节 |
| **后续** | 11 条，其中 **7 条标「永不做」**、**3 条给出「下版做什么已定死」**（情绪日记给了 key `xinyu.mood.log` 与字段形态；音频给了「1 条 3 分钟身体扫描」；UGC 与小程序给了触发条件） | 第 9 节 |

✅ **通过**。没有一条落进「以后再说」。

### 检查 3 · 至少有三个页面或视图的初步规划

实际 **11 个**，每个含优先级、承载功能、关键元素（第 6 节）：

`index.html` / `test.html` / `result.html` / `checkup.html` / `breathe.html` / `abc.html` / `records.html` / `articles.html` / `article.html` / `help.html` / `about.html`

另有全局组件表（导航 / 常驻危机入口 / 页脚）。

✅ **通过**（超出要求 3 倍）。

### 检查 4（老板第二条指令）· 每个 MVP 功能能否在一天内拆成小任务

**第 11 节，18 个单日任务**，每条含「产出文件 / 依赖 / 当天能验证什么」。抽查三个：

| 抽查功能 | 拆成 | 单日可验证的产出 | 判定 |
|---|---|---|---|
| F3 呼吸练习 | T5（1 天） | 吸 4/屏 7/呼 8 计时正确；暂停不跳帧；3 循环后停下 | ✅ 一天可交付 |
| F5 认知纠正 | T11 + T12（2 天） | T11 五步表单可走通；T12 保存后立即可见、刷新不丢、可删单条、空状态正常 | ✅ 拆得开，不跨功能 |
| F6 知识库 4 篇 | T13 + T14 + T15（3 天） | T13 列表可点进详情；T14 第 1 篇 ≥ 800 字；T15 其余 3 篇 | ✅ 拆得开 |

**规则明确写在第 11.1 节**：一人 × 一天（约 4 小时）、只碰 1~2 个文件、不跨功能、依赖必须标出。

✅ **通过**。并额外给出**排期不够时的砍除顺序**（第 11.3 节），以及 **6 项永久不可砍项**（骨架 / 危机页 / 自评 / 关于页 / 合规自查 / 部署）。

---

## 二、「必须修改」级问题（3 个，均已在写入时当场修正）

### 必须修改 1：`checkup.html` 在页面列表里存在，但功能需求里没有它

- **问题**：v2.0 第 6 节列了「情绪自评页」，第 7 节有 `xinyu.checkup.*` 两个 key，第 10.1 节有它的验收条目——**但第 5 节功能清单里找不到对应功能定义**。它被标成「F1 延伸」，而 F1 是人格测评，延伸不出情绪自评。
- **为什么是「必须修改」**：一个页面有存储字段、有验收条件、有危机触发逻辑，却没有功能定义——做的人会自己编，验收时无从对照。这是结构漏洞，不是瑕疵。
- **修正**：**F2 情绪自评独立成功能**（P0），补齐「谁/在哪/做什么/看到什么」+ 8 条验收标准 + 三种固定文案表；后续功能编号顺延（原 F2 安抚工具包拆成 F3 呼吸练习、F4 随机安慰卡）；同步修正第 5 节标题、第 6 节、第 11.2 节、第 14 节全部交叉引用。

### 必须修改 2（老板第一条指令）：与 `research.md` 的差异没有说清

- **问题**：`research.md` 第五节定的第一版范围是 **3 件事**（测评 / 呼吸+安慰卡 / 文章）。v2.0 悄悄变成了认知纠正 + 情绪自评 + 知识库。这**违反了自己在 `research.md` 里立的规矩**——当时明确写过「不给没有任何标注的数字与推断」。
- **为什么是「必须修改」**：文档之间可以不一致，但**不允许不说话**。用户在 Day 3 花了一天做的研究，到 Day 4 被无声覆盖，等于那份研究白做。
- **修正**：新增**第 2.4 节「与 research.md 的差异」**，用对照表逐条列出 2 项扩展（认知纠正、情绪自评）+ 1 项数量调整（文章 3~5 篇 → 固定 4 篇），每条写清「为什么扩 / 代价是什么」；并声明「research.md 第四节 9 条不做项已全部保留，无一条被悄悄拿掉」。

### 必须修改 3（老板第三条指令）：9 处「以后再说但没有边界」的表述

- **问题**（逐处列出）：

| # | 位置 | 原文 | 为什么不能留 |
|---|---|---|---|
| 1 | 第 3.2 节 | 「不做情绪日记」 | 与第 9 节「情绪日记 v2 主打候选」直接矛盾——到底做不做 |
| 2 | 第 9 节 | 「需要登录与多设备同步才有意义」（情绪日记） | 只说了困难，没说结论 |
| 3 | 第 9 节 | 「超出 20 天排期」（音频） | 排期不是理由，是借口。下一版排期更松，是不是就做了？ |
| 4 | 第 9 节 | 「看缘分」（小程序/App） | 这不是决策，是回避 |
| 5 | 第 9 节 | 「v2 再议」×3 | 议什么？议到哪一步？ |
| 6 | 第 10.1 节 | 「措辞不吓人、不含糊、不推卸」 | 无法判定，验收时只能靠感觉 |
| 7 | 第 10.1 节 | 「不弹窗、不打断、不吓人」（危机提示） | 同上 |
| 8 | 第 11 节 | 「其余均可砍」 | 没说哪些可砍、砍到什么程度、砍完还成不成立 |
| 9 | 第 13 节 | 「我的建议是**要**」（请临床的人） | 建议不算决策，不定会怎样没说 |

- **修正**：全部改成三选一——**「永不做」/「本版不做，下版做什么已定死」/「本版不做，触发条件是什么」**，并在第 9 节开头把这条写成硬规则。逐处结果：
  - 情绪日记 → 本版不做 + **下版首功能已定死**（形态：情绪打分 + 一句话备注的日历视图；key `xinyu.mood.log`；为什么本版不做：无数据导出方案，长期记录的价值立不住；触发条件：ABCDE 记录有用户重复使用 ≥ 2 次）
  - 音频 → 本版不做 + **下版做什么已定死**（只做 1 条 3 分钟身体扫描，不做曲库）
  - UGC / 小程序 → **给触发条件**（UGC：≥ 20 位真实用户要求投稿 + 确定 1 名每周 ≤ 2 小时审核人；小程序：30 天自然访问 ≥ 500 次）
  - 7 条改标「永不做」并各给一句理由
  - 可判定的措辞：`不吓人` → 改为「视觉与全站一致，不用红色警示色、无警报感」+ 对比度 ≥ 4.5:1；`不推卸` → 改为「不出现『后果自负』式免责」；`其余均可砍` → 换成第 11.3 节的**精确砍除顺序 + 6 项永久不可砍清单**

---

## 三、我主动指出的两个文档自身隐患（已在本版处理）

### 隐患 1：v2.0 到处都是「P1 / P2 可降级」，20 天排期里约等于事后不做

- **我的判断**：写「P2 可降级」的时候感觉是留了余地，实际是**把「要不要做」这个决断推给了未来的自己**。而未来的自己在赶工期。
- **处理**：第 8 节末尾加一条警告——**本版不设 P1/P2 降级项，9 个功能 11 个页面全部 P0**；排期不够只能按第 11.3 节的固定顺序砍，不允许自行判断「这个优先级低先不做」。

### 隐患 2：「合规必需」与「可降级」撞在一起会出事

- **我的判断**：v2.0 把危机资源页标 P0，却又在第 12 节风险表写「唯一不可降级的是危机资源页与免责内容，其余均可砍」——同一件事在一份文档里说两遍，但**「其余」里包含情绪自评，而自评是 L2/L3 用户被识别出来的唯一通道**。砍掉它，第 0 节的三级分层就成了一句空话。
- **处理**：第 11.3 节把砍除顺序写死，并在末尾单列 **6 项永久不可砍**（T1 骨架 / T2 危机页 / T9+T10 自评 / T16 关于页 / T17 合规自查 / T18 部署），明确写「砍掉任何一项，本站就不该上线」。

---

## 四、通过项核对表

| # | 检查项 | 结果 |
|---|---|---|
| 1 | 含验收标准 | ✅ 第 5 节每功能自带 + 第 10 节整站级分五类 |
| 2 | 验收标准可逐条检查 | ✅ 全部为布尔判断或可计数指标（「≤ 2 次点击」「≥ 8 条」「30 条」「≤ 1 分钟」「18 个任务」） |
| 3 | 能看出是哪个项目 | ✅ 标题 + 第 1 节项目名，正文多处「心屿」 |
| 4 | 每个功能有「谁/在哪/做什么/看到什么」 | ✅ 8 处，逐条验证 |
| 5 | 有页面/视图列表 | ✅ 第 6 节 11 页 + 全局组件表 |
| 6 | 有数据字段 | ✅ 第 7 节 7 个 key，含字段构成、写入时机、所属功能、5 条约束 |
| 7 | MVP 与后续有分界 | ✅ 第 5 节（MVP）vs 第 9 节（后续，7 条永不做 + 3 条已定死） |
| 8 | 每个功能可拆单日任务 | ✅ 第 11 节 18 个任务 |
| 9 | 与 research.md 一致或有显式说明 | ✅ 第 2.4 节对照表 |
| 10 | 无「以后再说」类空泛表述 | ✅ 全文检索无残留（第 9 节开头那句是规则本身） |
| 11 | 未写代码 | ✅ 全文无代码块，仅文件路径与 key 命名 |
| 12 | 未越界做 Day 5 技术选型 | ✅ 量表版本明确延到 Day 5，第 14 节第 1 条标了「不定会怎样」 |

---

## 五、结论

- **「必须修改」级问题：3 个，均已在写入 PRD 时当场修正**，无遗留阻塞。
- 达到 Day 4 完成标准：PRD.md 已入库、含可逐条检查的验收标准、自检挑不出未处理的「必须修改」级问题。
- 老板本轮四条收尾检查：**全部通过**（证据见第一节）。
- 保留事项：第 14 节 7 条待确认，每条都标了「定到什么时候」与「不定会怎样」，其中第 3 条（校心理咨询中心信息）会阻塞 T2，而 T2 是永久不可砍项——**这是当前最紧的一个依赖**。
