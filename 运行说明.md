# 心屿 · 本地运行说明

> Day 7 存档。照着做，2 分钟能在浏览器里打开网站。

## 怎么跑起来

前提：电脑上装了 Node.js（本机已装 v22.22.2）。

```
cd frontend
npm install        # 第一次跑之前装依赖，之后可跳过
npm run dev
```

看到类似 `Local: http://localhost:5173/` 的输出后，浏览器打开那个地址即可。

停止：在命令行窗口按 `Ctrl + C`。

## 今天能看到的页面

| 地址 | 页面 | 状态 |
|---|---|---|
| `/` | 分区首页（F11：三区 + 主 CTA） | ✅ 已实现骨架 |
| `/help` | 危机资源页（F12：120/110/12356 + 全国/地区/面诊三段） | ✅ 已实现 |
| 其余 11 个页面 | 统一占位页（可从导航/首页点进去，不会白屏） | 按排期逐个上线 |

## 页面里已经守住的规矩

- 任意页面 → 危机页 ≤ 1 次点击（导航「求助」+ 页脚常驻入口）
- 危机页首屏不滚动即可见 120 / 110 / 12356，号码可点拨号（`tel:` 链接）
- 分区标题逐字一致：「现在有点难受」「想弄明白自己」「想看点东西」
- 全站无红色警示色；危机页视觉无警报感

## 已知边界（不是 bug）

- 今天只有前端骨架，云函数 / 数据库还没接（云账号归属未拍板，不越界）
- 地区热线 8 条按 PRD 定案录入（核实日期 2026-09-22），**部署上线前需按 T2b 逐条重核**
- 根目录旧占位 `index.html` 已删除，由 `frontend/` 的 React 应用取代（历史里可追溯 `f0f4033`）
